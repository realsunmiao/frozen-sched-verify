# 课题:鸿蒙 PC 后台应用冻结态调度机制设计

> 来源:技术方案文档(单页长图)
> 目的:交给海外模型解题,后续由我方验证方案

---

## 一、技术背景

### 场景描述
- PC 应用**最小化**后只需保持最低功能,同时**回收其资源**(CPU、内存、网络等)供前台应用使用。

### 移动端现有方案
手机解决方案以**进程冻结**为核心,但直接搬到 PC 端会造成:
- 通知暂停
- 下载暂停
- 模型推理暂停

**问题**:与用户预期不符。

### 典型应用举例
腾讯会议、飞书、优酷、IE 浏览器、百度网盘、小艺

### 用户预期 vs 实际效果

| 切后台 → | 期待效果 | 实际效果 |
|---|---|---|
| 1 | 有通知 | 进程冻结 |
| 2 | 后台下载 | → 无算力供给 |
| 3 | 慢速推理 | → 通知、下载暂停 |

---

## 二、技术挑战

### 现有实现策略对比

| 方案 | 优/缺点 |
|---|---|
| 默认不冻结,低速运转 | **缺点**:后台开销大 |
| 默认冻结 + 重点应用白名单 | **缺点**:白名单规则不清晰,后台开销也较大 |
| 默认冻结 + 统一的服务进程接收通知 | **优点**:负载低、体验好<br>**缺点**:需要应用 + 推送服务供应方配合 |

### 鸿蒙 PC 落地的核心难点

- 鸿蒙 PC **场景多样**,**白名单局限很大**
- 系统统一接管,需要增加**系统接口 + 应用适配**
- **阻碍鸿蒙推广**

---

## 三、当前结果:通过调度算法解决"功能 vs 资源开销"的平衡

### 3.1 单一后台应用 —— 低维调参

**模型要素**:
- **app 指令序列示例**:a1, a2, m5, m7, a3, m5
- 内存 / 闪存两层存储,指令在两层间换入换出

**建模变量**:
- 已知量:**S** = app 指令
- 控制量:**C** = CPU 供给,**L** = 内存限制

**调度目标**:
- 内存 **M ~= L**
- 功能 **U = U(C)**
- 换出量 **H = H(C, L)**

### 3.2 多后台应用 —— 贪时调度策略

**场景示意**:
- 前台:app1
- 后台 group1:app2, app3
- 后台 group2:app4
- 多个 app 在 core1 / core2 上的时分复用调度

**建模变量**:
- **输入**:S1, S2, …, Sn(各 app 内存行为)
- **求解**:
  - C1(t), C2(t), … CPU 资源供给策略
  - L1(t), L2(t), … 内存资源供给策略
- **目标**:`min{M}` —— 总内存开销最小
- **约束**:
  - Ui > Uim(各 app 功能正常)
  - H < Hm(总换出量小于限额)

**核心挑战**:
> 建模刻画出应用对内存的访问,并找到**最优的调度策略**,使得各后台应用**物理内存开销最小**。

---

## 四、技术诉求

### 目标
设计一种**冻结态调度机制**

### 要求(三条硬性指标)
1. **应用无感免适配** —— 保障应用后台功能,不需要应用方改造
2. 后台内存开销 <= **同应用前台内存的 10%**
3. 内存换出量 **每日限额 ~100GB 量级**

### 验证步骤(三阶段)
1. **方案设计**
2. **安卓 PC / 手机上相似环境验证**
3. **鸿蒙 PC 上 TOP-k 应用同开情况下验证**,同时达成以上全部技术目标

---

## 五、给海外模型的关键问题(解题提示)

### 核心要回答的 4 问
1. **指令序列预测**:如何基于 app 历史行为(S = a1, a2, m5, m7, a3, m5)预测下一个时段的内存访问?
2. **最优 CPU/内存供给**:在 C(t) + L(t) 联合控制下,如何最小化 M 且满足 Ui > Uim?
3. **多 app 公平调度**:多个后台 app 共享 core1/core2,如何分配时间片让总换出量 H < Hm?
4. **冻结态恢复机制**:被冻结的 app 如何在 100GB/日 换出量预算下,无感恢复推理/下载/通知?

### 推荐输出格式
- **算法伪代码**(可含强化学习 / 预测模型 / 经典调度 ILP 任意一种)
- **关键数学模型**(目标函数 + 约束)
- **复杂度分析**
- **与三条硬性指标的对应关系**(逐条证明或估算)
- **TOP-k 应用同开场景下的预期 M、H 数值估算**

### 可参考的方向(不限定)
- 强化学习(PPO / DQN)做 CPU + 内存联合调度
- 时序预测(LSTM / Transformer)做指令序列预测
- 经典调度理论(EDF / 比例公平 / Lyapunov 优化)
- 内存压缩 + 换出策略(zram / zswap 类比)
- 冻结态分级(全冻结 / 半冻结 / 慢速运转)